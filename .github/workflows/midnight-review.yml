# 🌙 Midnight — Hermes CLI Investigation
# Runs at midnight: Hermes CLI (local Ollama model) autonomously browses
# the web, analyzes trends, and saves strategy to shared memory.
# Connected to daily morning run via hermes-memory/.

name: 🌙 Midnight Hermes Investigation

on:
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:

env:
  OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }}
  OPENROUTER_API_KEY_2: ${{ secrets.OPENROUTER_API_KEY_2 }}
  OPENROUTER_API_KEY_3: ${{ secrets.OPENROUTER_API_KEY_3 }}
  OPENROUTER_API_KEY_4: ${{ secrets.OPENROUTER_API_KEY_4 }}
  DISCORD_BOT_TOKEN: ${{ secrets.DISCORD_BOT_TOKEN }}
  DISCORD_CHANNEL_ID: ${{ secrets.DISCORD_CHANNEL_ID }}
  DISCORD_USER_ID: ${{ secrets.DISCORD_USER_ID }}
  GH_PAT: ${{ secrets.GH_PAT }}

jobs:
  hermes-investigation:
    runs-on: ubuntu-latest
    timeout-minutes: 60

    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GH_PAT || secrets.GITHUB_TOKEN }}
          fetch-depth: 0

      - uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install deps
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y -qq ffmpeg jq curl
          npm ci || npm install

      - name: Install Ollama + pull local model
        run: |
          curl -fsSL https://ollama.com/install.sh | sh
          ollama serve > ollama.log 2>&1 &
          sleep 10
          ollama pull llama3.2:3b
          echo "OLLAMA_HOST=http://localhost:11434" >> $GITHUB_ENV

      - name: Install Hermes CLI
        run: |
          curl -fsSL https://hermes-agent.nousresearch.com/install.sh | bash
          mkdir -p ~/.hermes
          cat > ~/.hermes/config.yaml <<'EOF'
          model:
            provider: custom
            default: llama3.2:3b
            base_url: http://localhost:11434/v1
            api_key: ollama
            api_mode: chat_completions
            ollama_compatible: true
            context_length: 65536
          agent:
            max_turns: 4
          toolsets:
            - web
            - terminal
            - skills
          memory:
            enabled: true
            char_limit: 2000
          EOF
          hermes status || true
          echo "HERMES_CONFIG=$HOME/.hermes/config.yaml" >> $GITHUB_ENV

      - name: Restore Hermes memory from previous runs
        run: |
          mkdir -p ~/.hermes
          if [ -d "hermes-memory" ]; then cp -r hermes-memory/* ~/.hermes/ 2>/dev/null || true; fi

      - name: Run Hermes CLI investigation
        run: node core/github-action-runner.js --mode nightly

      - name: Save memory
        run: |
          cp -r ~/.hermes/* hermes-memory/ 2>/dev/null || true
          git config user.name "Hermes Agent"
          git config user.email "hermes@worldwidewebster.com"
          git add memory/ hermes-memory/ 2>/dev/null || true
          if ! git diff --staged --quiet; then
            git commit -m "🌙 Hermes nightly: $(date +%Y-%m-%d)"
            git push
          fi

      - name: Cleanup
        if: always()
        run: pkill ollama 2>/dev/null || true